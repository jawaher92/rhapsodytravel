{% if is_granted('ROLE_ADMIN') %}

{% extends 'simpleTemplate.html.twig' %}
{% block title %}Mise à Jour{% endblock %}

{% block editevent %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}
{% endif %}